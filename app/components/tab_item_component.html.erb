<li>
  <%= link_to admin_conversations_path(assignee_type: assignee_type), class: "pb-2 block #{active ? 'border-b-2 text-purple-600 border-purple-600' : 'text-gray-300'}" do %>
    <%= name %> <span class="px-1.5 py-0.5 text-xs rounded-full <%= active ? 'bg-purple-200' : 'bg-gray-100' %>"><%= count %></span>
  <% end %>
</li>
