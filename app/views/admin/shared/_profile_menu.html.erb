<div class="cursor-pointer" data-controller="dropdown">
  <div data-dropdown-target="template" class="hidden flex flex-col text-base space-y-3 w-60">
    <%= link_to "Profile", destroy_admin_session_path, method: :delete %>
    <%= link_to "Widget Code", "#", data: { reflex: "click->WidgetReflex#open_code" } %>
    <%= link_to "Settings", destroy_admin_session_path, method: :delete %>
    <%= link_to "Sign Out", destroy_admin_session_path, method: :delete %>
  </div>
  <div class="w-12 h-12 overflow-hidden rounded-full">
    <%= render(AvatarComponent.new(user: current_admin)) %>
  </div>
</div>
